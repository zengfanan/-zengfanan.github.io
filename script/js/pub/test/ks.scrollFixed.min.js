!function (a) {
	"function" == typeof define ? define(a) : a("", window.ue = window.ue || {}, "")
}
(function (a, b, c) {
	function d() {
		return {
			width : $(document).width(),
			height : window.innerHeight || document.documentElement.clientHeight
		}
	}
	function e() {
		return document.documentElement.scrollTop || document.body.scrollTop
	}
	function f(a) {
		if (this.constructor !== f)
			return new f(a);
		var b = {
			target : "",
			top : 10,
			left : 20
		};
		a = $.extend(b, a),
		i.push(a),
		1 == i.length && (j ? setInterval(g, 400) : setInterval(h, 50))
	}
	function g() {
		for (var a = (d().height, e(), 0); a < i.length; a++)
			e() > i[a].top ? (i[a].target.css({
					position : "absolute"
				}), i[a].target.stop().animate({
					top : e()
				}, 300)) : i[a].target.css({
				position : "static"
			})
	}
	function h() {
		for (var a = (d().height, e(), 0); a < i.length; a++)
			i[a].target.css(e() > i[a].top ? {
				position : "fixed",
				top : 0
			}
				 : {
				position : "static"
			})
	} {
		var i = [],
		j = $.browser.msie && "6.0" == $.browser.version;
		new Date
	}
	"[object Object]" == {}

	.toString.call(c) ? c.exports = f : b.scrollFixed = f
});
